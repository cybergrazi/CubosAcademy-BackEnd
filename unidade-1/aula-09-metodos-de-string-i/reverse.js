// Introdução ao método reverse.

const numeros = [1, 2, 3, 4, 5];
console.log(numeros);

const resultado = numeros.reverse();

console.log(resultado);
console.log(numeros); // muta também o original.