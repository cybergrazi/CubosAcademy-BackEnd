// Introdução à estrutura de repetição while.

const nomeEstudantes = ["Maria", "José", "Fernanda", "Catarina", "Elizabeth"];

let indice = 0;

while (indice < nomeEstudantes.length) {
    console.log(nomeEstudantes[indice]);
    indice++;
}