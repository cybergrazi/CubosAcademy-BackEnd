// Introdução ao conceito de objetos.

const pessoa = {  // é assim que fica guardado o objeto.
    nome: "Grazi",
    altura: 1.69,
    peso: 75
};

console.log(pessoa);
console.log(`Meu nome é ${pessoa["nome"]}, minha altura é ${pessoa.altura} e meu peso é ${pessoa.peso}.`)